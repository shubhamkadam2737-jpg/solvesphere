import { Link, useParams } from 'react-router-dom'
import { challenges } from '../data/mockData'
export function ChallengeDetails(){const {id}=useParams();const c=challenges.find(x=>x.id===Number(id));if(!c)return <section className="page"><h1>Challenge not found</h1><Link to="/challenges">Back to challenges</Link></section>;return <section className="page"><Link className="text-link" to="/challenges">← Back to challenges</Link><div className="panel detail"><div><span className={`badge ${c.priority.toLowerCase()}`}>{c.priority}</span><span className="badge">{c.status}</span></div><h1>{c.title}</h1><p>{c.category} • {c.location}</p><p>Reported by {c.owner}. This challenge currently has {c.teams} teams and {c.evidence} evidence items.</p><div className="progress"><i style={{width:`${c.readiness}%`}}/></div><strong>{c.readiness}% solution readiness</strong><div className="detail-actions"><button className="btn btn-primary">Join a team</button><button className="btn btn-secondary">Report / update evidence</button></div></div></section>}
