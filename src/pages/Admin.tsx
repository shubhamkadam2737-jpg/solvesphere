import { challenges, solutions, teams } from '../data/mockData'
export function Admin(){return <section className="page"><div className="page-head"><div><span className="eyebrow">Platform governance</span><h1>Admin console</h1><p>Review challenges, prioritize work and monitor the overall platform.</p></div></div><div className="stats">{[['Challenges',challenges.length],['Critical',challenges.filter(c=>c.priority==='Critical').length],['Teams',teams.length],['Solutions',solutions.length]].map(([l,v])=><article key={String(l)}><small>{l}</small><strong>{v as number}</strong></article>)}</div><div className="grid"><div className="panel"><h2>Challenge moderation</h2>{challenges.map(c=><div className="row" key={c.id}><div><b>{c.title}</b><small>{c.location}</small></div><span className={`badge ${c.priority.toLowerCase()}`}>{c.priority}</span><button className="btn btn-secondary">Review</button></div>)}</div><div className="panel"><h2>Solution review queue</h2>{solutions.map(s=><div className="row" key={s.id}><div><b>{s.title}</b><small>{s.team}</small></div><span className="badge">{s.status}</span></div>)}</div></div></section>}
