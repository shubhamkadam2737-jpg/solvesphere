import { useAuth } from '../context/AuthContext'
export function Profile(){const {user}=useAuth();return <section className="page"><div className="page-head"><div><span className="eyebrow">Your identity</span><h1>Profile</h1><p>Manage your SolveSphere contribution profile.</p></div></div><div className="panel profile"><div className="avatar-lg">{user?.name.slice(0,1).toUpperCase()}</div><h2>{user?.name}</h2><span className="badge">{user?.role}</span><p>{user?.email}</p><hr/><div className="profile-grid"><div><small>Role</small><strong>{user?.role}</strong></div><div><small>Member ID</small><strong>{user?.id}</strong></div></div></div></section>}
