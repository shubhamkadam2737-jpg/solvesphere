import { solutions } from '../data/mockData'
export function Solutions(){return <section className="page"><div className="page-head"><div><span className="eyebrow">Solution lifecycle</span><h1>Solutions</h1><p>Submit, review and improve ideas until they are ready for real-world impact.</p></div><button className="btn btn-primary">Submit solution</button></div><div className="panel table">{solutions.map(s=><div className="row" key={s.id}><div><b>{s.title}</b><small>{s.team}</small></div><span className="badge">{s.status}</span><small>{s.feedback}</small></div>)}</div></section>}
